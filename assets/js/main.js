const toggle=document.querySelector('.nav-toggle');const nav=document.querySelector('.nav-links');if(toggle&&nav){toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open))});nav.addEventListener('click',e=>{if(e.target.tagName==='A'){nav.classList.remove('open');toggle.setAttribute('aria-expanded','false')}})}document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());const updates=document.querySelector('[data-updates]');if(updates){fetch('data/updates.json').then(r=>{if(!r.ok)throw new Error('Updates unavailable');return r.json()}).then(items=>{updates.innerHTML=items.map(item=>`<li><span class="date">${item.date}</span>${item.text}</li>`).join('')}).catch(()=>{updates.innerHTML='<li>Updates are temporarily unavailable.</li>'})}
